export type Blog = {
    title: string,
    subtitle?: string,
    imageUrl?: string,
    content: string,
    secondImageUrl?: string,
    description?: string,
    _id?: string,
    createdAt?: string,
    updatedAt?: string,
}